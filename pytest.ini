[pytest]
markers =
    ui: UI page load tests
    navigation: navigation and routing tests
    forms: form interaction tests
    auth: authentication and security header tests
    api: API endpoint tests
